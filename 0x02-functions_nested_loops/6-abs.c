#include "main.h"
/**
 * _abs - compute the absolute value of integer
 * @t: the intiger to be computed
 * Return: the computed value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
